/**
 * js中数组是动态大小的
 */

// 使用Array构造函数创建数组
// 创建包括有2个元素的数组
let array = new Array(20)
console.log(array)

// 数组字面量
// 不会调用Array构造函数
array = [1, 2, 3]

// Array.from(): 将类数组转换为数组

// 队列 栈 方法

// 队 先进先出
// push shift

// 反向 队列
// unshift pop


// 栈 入栈  出栈
// push pop
